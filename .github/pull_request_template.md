## Summary

<!--
The block below becomes the body of the GitHub release cut from this PR. The squash-merge
message is pre-filled from this description, and the release workflow reads the markers
out of the merge commit.

Write it for someone deciding whether to upgrade: what changed, why it matters, and
anything that requires action on their side.

The block is rendered as GitHub-flavoured Markdown, exactly as written — headings, lists,
tables, code fences, links and `#123` references all work. It is copied verbatim, so
anything that is not valid Markdown will look wrong in the published release.

Do not repeat the title or restate the date, PR number, or closed issues: this PR's title
becomes the heading, and a byline is derived from the commit and the PR's linked issues.
Start the block with your own `##` heading only if you want a different one, in which case
no byline is added.

Leave it empty for changes that do not warrant a release note — the release falls back to
the commit subject line, so nothing breaks either way. If you edit the message in the
merge dialog, keep the markers.
-->

<!-- release-notes -->

<!-- /release-notes -->

## Test plan
